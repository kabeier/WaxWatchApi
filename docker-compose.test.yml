services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: waxwatch_test
      POSTGRES_USER: waxwatch
      POSTGRES_PASSWORD: waxwatch
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U waxwatch -d waxwatch_test"]
      interval: 5s
      timeout: 3s
      retries: 20