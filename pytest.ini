[pytest]
testpaths = tests
addopts = -q --cov=app --cov-report=term-missing --cov-report=json:coverage.json --cov-fail-under=85
markers =
    integration: tests that require orchestrated external services (Celery/Redis smoke)
filterwarnings =
    ignore::DeprecationWarning
pythonpath = .
