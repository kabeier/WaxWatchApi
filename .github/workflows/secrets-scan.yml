name: Secrets Scan

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    name: Gitleaks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@<full_commit_sha> # v6.0.2
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@<full_commit_sha> # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
